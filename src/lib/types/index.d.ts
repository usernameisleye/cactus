export type BlockType = {
    id: number
    src: string
    body: string
    header: string
}
export type QRType = {
    bg: string
    fore: string
    data: string
    size: string
    format: string
}